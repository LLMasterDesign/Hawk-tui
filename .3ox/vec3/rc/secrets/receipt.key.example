# RECEIPT.KEY :: HMAC Secret for Receipt Integrity
# This key is used to sign receipts for tamper detection
# Generate a secure random key and keep it secret

# Example key (REPLACE WITH YOUR OWN SECURE KEY):
# To generate a secure key, run: openssl rand -hex 32
your_secure_hmac_key_here_32_chars_minimum

# Security Notes:
# - Keep this key secure and never share it
# - Use a strong, random key (at least 32 characters)
# - Rotate keys periodically for enhanced security
# - Backup securely but separately from the key itself